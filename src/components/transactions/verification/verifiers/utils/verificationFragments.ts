import { VerificationFragment } from "@govtechsg/oa-verify";

export const processVerificationFragments = (fragments: VerificationFragment[]) => {
  // This is now handled in the main verificationTypes.ts file
  return fragments;
};